Windows 
   1. Docker Desktop Install:
   2. Supabase CLI Install: Scoop  ||  npm install -g supabase

  2. Supabase
   1. ディレクトリ作って移動
   2. supabase init
   3. supabase start
   4. API URL (http://127.0.0.1:54321), anon key, service_role key → メモ

  3. Cloudflare Tunnel
   1. Cloudflare Dashboard (Zero Trust > Networks > Tunnels)
   2. Dashboard → Public Hostname → http://127.0.0.1:54321 (Supabase API)



  Linux CUI
   1. Docker Engine
   1     curl -fsSL https://get.docker.com -o get-docker.sh
   2     sudo sh get-docker.sh
   3     sudo usermod -aG docker $USER
   4     Re-log in  ||  newgrp docker

   2. Supabase CLI
   1     brew install supabase/tap/supabase   ||   npm install -g supabase

  3. Supabase
   1. mkdir ~/culture-fest-db && cd ~/culture-fest-db
   2. supabase init
   3. supabase start
   4. API URL (http://127.0.0.1:54321), anon key, service_role key → メモ

  3. Cloudflare Tunnel
   1. Cloudflare Dashboard (Zero Trust > Networks > Tunnels)
   2. sudo cloudflared service install <Token> 
   3. Dashboard → Public Hostname → http://127.0.0.1:54321 (Supabase API)



Supabase Power On
   1. Supabase Studio 
       Win: http://127.0.0.1:54323
       Lin: 192.168.x.x:54323 → MacBook
   2. SQL:
       server_setup.sql

スリープ, Windows Updateオフ